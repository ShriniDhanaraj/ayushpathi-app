{
  "name": "ayushpathi-app",
  "version": "0.1.0",
  "private": true,
  "description": "Ayushpathi — AYUSH Healthcare Platform (NGO)",
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "web": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
